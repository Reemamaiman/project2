package com.example.employees;

import java.util.ArrayList;

public class MerchantStockService {

    private ArrayList<ArrayList<Stock>> stock = new ArrayList<>();
    private ArrayList<ArrayList<Merchant>> MerchantStock = new ArrayList<>();

    public void addMerchantStock(Merchant Stock stock){
        stockList.add(stock);}

    public void updateMerchantStock(int index,  MerchantStock stock){
        stockList.set(index, stock);}

    public void deleteMerchantStock(int index){
        stockList.remove(index);}
    }
    private ArrayList<ArrayList<Stock>> stockList= new ArrayList<>();
    public MerchantStock addProduct(Product product, int id){
        for (int i = 0; i < stockList.size() ; i++) {
            if stockList.get(i);
            return  stockList.get(i);

            ArrayList<User> u =new ArrayList<>();
            if(u.get(i).getRole().equals("admin"))
                stockList.add(
                        return
                )


        }
}
